specVersion: "v1beta"
serviceId: "chat-web-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "chat-web-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
